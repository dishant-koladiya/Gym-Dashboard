# Gym-Dashboard
Prisma ORM => it acts as a bridge between an application's code and its database, allowing developers
              to read and write data using regular code rather than writing raw SQL queries  

.env => to store all connectivity link 
 

 npm run dev +  npx prisma studio

 api/auth/login 